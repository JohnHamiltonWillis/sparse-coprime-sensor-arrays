for spacing = 1:64
    coprime_pairs = GenerateCoprimePairs(2,64,spacing);
    for pair = 1:length(coprime_pairs)
        U1 = coprime_pairs{pair}(1);
        U2 = coprime_pairs{pair}(2);
        
    end
end
